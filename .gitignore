__pycache__/
*.pyc
build/
dist/
saves/
.venv/
venv/

# Stockfish binaries (~110 MB each, over GitHub's file size limit).
# Fetch them with: python download_stockfish.py
engines/linux/stockfish
engines/windows/stockfish.exe

# Test artifacts
tests/*.png

# Opening-data build cache (TWIC archives, intermediate counts) — only the
# final app/data/openings.json.gz is committed.
tools/cache/
